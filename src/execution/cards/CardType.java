package execution.cards;

/**
 * This data type represents all the cards that can exist in the game. In the future, the game might
 * add new card types.
 */
public enum CardType {
    HERO,
    MINION,
    ENVIRONMENT;
}
